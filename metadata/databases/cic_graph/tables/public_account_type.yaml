table:
  name: account_type
  schema: public
is_enum: true
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: account_type
        table:
          name: accounts
          schema: public
