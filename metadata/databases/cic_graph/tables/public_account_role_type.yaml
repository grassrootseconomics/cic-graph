table:
  name: account_role_type
  schema: public
is_enum: true
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: account_role
        table:
          name: accounts
          schema: public
